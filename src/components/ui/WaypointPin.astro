---
interface Props { number: number }
const { number } = Astro.props;
---
<span class="waypoint-pin relative z-10 flex h-11 w-11 shrink-0 items-center justify-center rounded-full bg-brand-500 text-sm font-mono font-bold text-white">{number}</span>
